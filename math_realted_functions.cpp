//math operations in c++
#include <iostream>
#include <cmath>

using namespace std;

int main()
{   
    int x = 20;
    int y = 35;
    int z;

    //z = max(x,y);
    //z = min(x,y);
    //z = pow(x,2);
    //z = sqrt(x);
    //z = round(3.5);//round up 
    //z = ceil(7.1);//round up
    z = floor(7.9);//round down


    cout <<z << '\n';
    //cout << "min = "<< Min << '\n'; 
    //cout << "power = "<< power << '\n';




    return 0;
}